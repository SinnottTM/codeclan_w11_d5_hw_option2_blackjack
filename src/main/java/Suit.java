// ENUMS are used for variables that will not be changed or added to except within this class
// ENUMS are their own datatype, unique from String or primitives

public enum Suit {
    HEARTS,
    DIAMONDS,
    SPADES,
    CLUBS,
}
